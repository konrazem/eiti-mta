{
  "authenticationMethod": "route",
  "routes": [
    {
      "destination": "eiti-server",
      "source": "^/api/v1/(.*)$",
      "authenticationType": "xsuaa"
    },
    {
      "destination": "eiti-app-build",
      "source": "^/app(.*)$",
      "authenticationType": "xsuaa"
    }
  ]
}